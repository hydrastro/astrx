# Rust
/target/
**/*.rs.bk

# Python (legacy-python/ during migration)
__pycache__/
*.py[cod]
*.egg-info/
build/
.pytest_cache/
.mypy_cache/

# Local runtime state — an index/tracker DB is machine-local, never committed
*.db
*.db-journal
*.db-wal
*.db-shm
peers.json

# Environments
.venv/
venv/
.env
