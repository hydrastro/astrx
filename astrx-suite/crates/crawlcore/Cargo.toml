[package]
name = "crawlcore"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Shared crawl library: robots glob, SimHash dedup, recrawl scheduling, trap predicates."

[dependencies]
