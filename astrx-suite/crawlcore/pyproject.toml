[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "crawlcore"
version = "1.0.0"
description = "Shared zero-dependency crawler mechanics for the astrx crawlers"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["crawlcore"]
