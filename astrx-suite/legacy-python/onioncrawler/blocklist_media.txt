# onioncrawler ABUSE MEDIA BLOCKLIST  (operator-maintained, REQUIRED)
# --------------------------------------------------------------------
# One hex SHA-256 digest per line (64 lowercase hex chars). When the crawler
# downloads a media / non-text resource, it computes the SHA-256 of the bytes
# and, on a match here, DROPS that page from the index and flags the whole host
# 'blocked' (its indexed pages then disappear from search). This is the
# Ahmia-grade media-hash defense against known-abusive imagery (esp. CSAM).
#
# Obtain hashes from a trusted hash-sharing programme (e.g. a hotline/NCMEC-style
# feed). Operators MUST maintain this; it ships EMPTY on purpose.
#
# Lines starting with '#' and blank lines are ignored. Example (do not use):
#   e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
