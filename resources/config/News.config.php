<?php
declare(strict_types=1);

return [
    'News' => [
        // Items shown per page. 0 = no pagination (show all).
        'per_page'   => 20,

        // Show newest items first.
        'descending' => true,
    ],
];