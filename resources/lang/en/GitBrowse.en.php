<?php
declare(strict_types=1);

/**
 * Git browser link-through — en locale.
 *
 * Loaded by GitBrowseController via loadDomain(langDir(), 'GitBrowse').
 * Keys mirror the it counterpart 1:1 (check_lang_parity.php).
 */
return [
    'gitbrowse.heading' => 'Browse the code',
    'gitbrowse.intro'   => 'Read-only source browsing is served by the standalone gitweb app.',
    'gitbrowse.open'    => 'Open the git browser',
    'gitbrowse.note'    => 'This opens the external gitweb service in a new tab. It is a separate application — AstrX only links to it.',
];
